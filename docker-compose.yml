version: "3.9"
services:
  web:
    build: .
    ports:
      - "3000:5000"
    volumes:
      - db_data:/app/data
    environment:
      - SECRET_KEY=change-me-in-production
      - DATABASE_URL=sqlite:////app/data/booking.db
    restart: unless-stopped

volumes:
  db_data:
